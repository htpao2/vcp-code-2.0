// @use-gesture/core/types is a type-only re-export path.
// Some published package variants miss runtime helper chunks for this entry,
// so we provide an empty runtime shim for webview bundling.
export {}
